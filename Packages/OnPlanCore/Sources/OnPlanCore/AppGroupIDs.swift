import Foundation

public enum AppGroupIDs {
    /// iPhone app + Home Screen widgets + SwiftData store.
    public static let iOS = "group.com.dailyonplan.tracker"
    /// macOS menu bar + desktop widgets (`TEAMID.name`).
    public static let macOS = "6998422DKP.com.dailyonplan.shared"
    /// Watch app + complications. Snapshot only.
    public static let watch = "group.com.dailyonplan.tracker.watch"
    public static let cloudKitContainer = "iCloud.com.dailyonplan.tracker"
}
